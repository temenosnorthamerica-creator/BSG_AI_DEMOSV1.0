{
  "name": "esb-application",
  "version": "1.0.0",
  "description": "Enterprise Service Bus Application",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "start": "echo Starting Backend... && start cmd /k \"cd backend && npm run dev\" && timeout /t 2 && echo Starting Frontend... && cd frontend && npm run dev"
  },
  "keywords": ["esb", "integration", "api"],
  "author": "",
  "license": "ISC"
}
